# 14 — 审查修复：运行时清理、空房判定与恢复文案（应用层）

**What to build:** 修复代码审查发现：房间拆除逻辑去重、重复调用删除、进行中房的空房判定与「本地人类恒有效」一致、恢复失败展示友好文案、死代码/过渡接口清理。

**Blocked by:** None — 可立即开工（审查后续修复）

**Status:** done

- [ ] 房间拆除动作（registry/持久化/运行时/presence/活动计时）提取为单一复用函数，两处调用点收敛
- [ ] 同一块内重复的「房间活动标记」调用删除
- [ ] 进行中房空房判定修正：本地人类座位（含已淘汰）恒有效；远程座位仅未淘汰且未进入离席/超时才有效
- [ ] 恢复失败前端不再直接展示原始错误码：已知错误码映射为友好中文文案
- [ ] 生产零调用的过渡接口与死代码清理（房间持久化过渡适配器、无调用方的存储接口），测试改用正式接口；规格点名保留的按房查询能力不动
- [ ] 回归：全量 typecheck + 测试全绿

## Comments

- 完成：抽取统一房间解散清理函数，删除重复活动标记，修正进行中房本地淘汰座位的空房语义。
- 完成：恢复失败原因改为友好中文摘要，移除房间持久化过渡适配器并将测试迁移到正式接口；保留按房间查询进行中 run 与调试注入能力。
- 测试：`npm run typecheck`、`npm test` 全部通过。
- Commit: `e14e592` (`Fix runtime room cleanup and recovery handling`)
