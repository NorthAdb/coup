# 13 — 审查修复：迁移失败可见性与非空约束（持久化层）

**What to build:** 修复代码审查发现：迁移失败必须端到端可见（不再静默吞错）；无法归属的旧 run 无论 legacy 是否损坏都要标记迁移异常；迁移 SQL 不再跨模块直写对局表；`room_code` 非空约束落实到数据库层。

**Blocked by:** None — 可立即开工（审查后续修复）

**Status:** done

- [ ] legacy 房间数据损坏时：保留旧数据、启动进入显式迁移失败状态、恢复 API 可见且可放弃，不得静默
- [ ] 无法归属的旧 run 标记迁移异常与 legacy 解析成败解耦：legacy 损坏时同样执行
- [ ] 迁移逻辑不再由房间持久化模块直接写对局运行表；错误不再被吞掉
- [ ] 对局运行表 `room_code` 非空约束落地（数据库层 + 创建入口拒绝缺省）
- [ ] 覆盖：legacy 损坏的端到端场景（应用工厂级）、非空约束、迁移异常标记完整性

## Comments

- 完成迁移编排修复：房间存储不再直写对局运行表，应用启动统一协调 legacy 房间与运行迁移；迁移级失败通过恢复 API 暴露并可逐项放弃。
- 存量 `match_runs.room_code` 迁移为数据库 `NOT NULL`，无法归属的旧 run 标记为 `migration_error`；创建和续局入口拒绝缺省房号。
- 测试：`npm run typecheck`、`npm test` 全部通过（全量测试 143 个通过）。
- Commit: `d622427`
