# 决策说明作为本局旁路，不进权威历史

早期座位协议要求 Agent 只返回结构化 `SeatDecision`，界面在思考阶段仅显示状态提示、不展示理由，以免泄漏私有推理并污染权威事件流。为满足「决策后能理解 Agent 为何如此选择」而不打开原始思维链，我们改为：适配器可附带一句可选的决策说明，经校验决策后仅保存在本局内存供座位 hover；缺失时用结构化决策的模板文案兜底并标明来源。决策说明不是领域事件，不写入 SQLite，不进入回放；思考中仍只显示阶段提示。原始 transcript / 思维链仍然禁止展示与持久化。
