# 12 — 逐房启动恢复与逐房放弃

**What to build:** 服务器重启后按房间全量恢复（大厅、续局等待、进行中对局）；单房恢复失败独立标记且不影响其他房；放弃接口携带房号只清理该房；前端恢复界面适配多房结果。

**Blocked by:** 05 持久化表化与旧数据迁移, 06 移除单 run 不变量, 07 presence 房间作用域键, 09 per-room 对局运行时与房间作用域对局 API（服务端）

**Status:** done

- [ ] 重启恢复全部房间：大厅/续局等待直接恢复；进行中房恢复原对局且不新建 run
- [ ] 一房恢复失败只标记该房（含房号、原因、可展示摘要），其他房照常服务
- [ ] 恢复查询返回全部房间结果；放弃请求携带房号，只清理该房的持久化、运行时、在场状态与失败状态
- [ ] 放弃一房不影响其他房的恢复状态与运行
- [ ] 前端能展示多房恢复状态并逐房放弃

## Comments

- 已完成逐房启动恢复：大厅、续局等待与进行中对局按房号独立恢复；进行中对局复用原 run，恢复失败独立记录房号、原因与摘要，其他房间继续服务。
- `/api/room-recovery` 返回全部房间结果；放弃接口要求 `roomCode`，仅清理目标房间的持久化、运行时、presence 与恢复状态。前端展示多房状态并支持逐房放弃。
- 验证：`npm run typecheck` 通过；`npm test` 全量 75 tests 通过；恢复单文件测试 7 tests 通过。
- 实现提交：`58f8296`。
