# 结果节拍用表现层回放，不阻塞权威推进

一次人类决策后，服务端会在同请求内推进后续 Agent 直至再次轮到人类，客户端只收到最终快照——若立刻跳到当前阶段，玩家会被迫盯右侧对局记录才能理解刚发生的连锁结果。我们采用表现层结果节拍：按新增公开事件轻重两档在中央舞台播「刚才发生」旁白并短暂停顿（可叠座位呼出），权威状态不倒带、不改 Agent 协议；否决了服务端遇拍即停（需 ACK/改运行时）。右侧对局记录仍只做回顾。
