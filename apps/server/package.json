{
  "name": "@coup/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "npm run build && node dist/main.js",
    "test": "npm run build && node --test dist/startServer.test.js"
  },
  "dependencies": {
    "@fastify/static": "^8.2.0",
    "fastify": "^5.5.0",
    "open": "^10.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.17.2",
    "typescript": "^5.9.2"
  }
}
